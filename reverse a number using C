#include <stdio.h>

int main()
{
   int n,rev=0,lastdigit;
   scanf("%d",&n);
   while(n>0)
   {
      lastdigit=n%10;
      rev=(rev*10) + lastdigit;
      n=n/10;
      
   }
    printf("%d",rev);
    return 0;
}
